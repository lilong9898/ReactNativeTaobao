language: android
before_install:
- nvm install 6
- node --version
install:
- npm install
android:
  components:
  - build-tools-23.0.1
  - android-23
  - extra-android-m2repository

script: 
- npm run ci
- cd android && ./gradlew assembleRelease
